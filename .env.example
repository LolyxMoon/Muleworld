# PartyKit host — set this after deploying:
# npx partykit deploy party/server.js --name mulerun-world
# Then your host will be: mulerun-world.YOUR_USERNAME.partykit.dev

VITE_PARTYKIT_HOST=localhost:1999
